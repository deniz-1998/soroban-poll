[package]
name = "soroban-poll-contract"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "20.0.0" }

[profile.release]
opt-level = "z"
overflow-checks = true
strip = true
